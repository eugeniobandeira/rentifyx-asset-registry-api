# Project State

## Last Updated

2026-07-24

## Current Work

**2026-07-24 session, continued: CI confirmed fully green on `main` for the first time since 2026-07-23 (run `30121225823`) — G-004 genuinely closed, not just believed closed.** The initial suppression file (below) still failed a real CI run: it only covered `OpenTelemetry`/`OpenTelemetry.Instrumentation.AspNetCore` package names, but the actual scan flagged `OpenTelemetry.Api.dll`/`OpenTelemetry.Extensions.Hosting.dll` too — same CVE-2026-39883, same already-documented Go-project false positive, just sibling packages the original regex's narrower scope missed (this repo also pins `OpenTelemetry.Exporter.OpenTelemetryProtocol`/`Instrumentation.Http`/`Instrumentation.Runtime`, all 1.17.0). Widened the `packageUrl` regex to `^pkg:generic/OpenTelemetry(\..+)?@.*$` to cover the whole package family. Re-ran on push: `build-and-test`, `OWASP dependency-check`, and `Trivy container scan` all passed — first fully green run on this gate since D-003 first went red. Lesson worth keeping: a suppression file authored from one CI report's specific findings can still miss sibling packages/DLLs not present in that exact snapshot — only a real subsequent CI run proves it actually works, "looks right" isn't enough.

**2026-07-24 session, continued: G-004 resolved — real OWASP dependency-check suppression file authored from an actual CI report artifact, not guessed CVEs.** Downloaded the `dependency-check-report.html` artifact from the most recent `main` CI run (`gh run download 30110991990 -n dependency-check-report`) and parsed it for the exact CVE IDs/CVSS scores/CPEs currently flagged at ≥7, per the task's explicit instruction not to fabricate findings. Confirmed the report's own dashboard count (`vulnerableCount = 6`) matches the 6 dependencies previously named in D-003 (Google.Protobuf, JsonPointer.Net, Microsoft.NET.StringTools, Nerdbank.MessagePack, OpenTelemetry, OpenTelemetry.Instrumentation.AspNetCore) plus surfaced the exact CVE IDs D-003 hadn't recorded (13 distinct CVEs on Nerdbank.MessagePack alone). Each finding's own description text was read to confirm the false-positive reasoning already suspected in D-003 rather than assuming it held: Google.Protobuf/JsonPointer.Net/OpenTelemetry findings all describe a different project than the .NET NuGet package flagged (Python protobuf, an unrelated npm package, OpenTelemetry-Go); Nerdbank.MessagePack's 13 CVEs describe the separate MessagePack-CSharp project or a Go msgpack library; Microsoft.NET.StringTools.dll isn't a package this repo references at all (confirmed absent from `Directory.Packages.props`) — it ships inside the .NET SDK. Before suppressing anything, checked the NuGet flat-container API for each of the 3 packages this repo directly pins (Google.Protobuf, Nerdbank.MessagePack, JsonPointer.Net) to confirm no newer *stable* version existed to bump to instead — all three were already at the latest stable release, so suppression (not a version bump) was the correct call for all of them, consistent with the task's "don't suppress what's fixable" instruction. Wrote `dependency-check-suppressions.xml` at repo root (schema verified against the tool's own docs/XSD via WebFetch, including confirming `<suppress>` blocks support multiple repeated `<cve>` elements — used for Nerdbank.MessagePack's 13 CVEs in one block keyed off a `packageUrl regex`), wired into `.github/workflows/ci.yml`'s dependency-check step via `--suppression dependency-check-suppressions.xml`. Validated XML well-formed (PowerShell `XmlDocument.Load`); could not run a real CI job from this worktree/branch to confirm the gate goes green (not pushed, per instructions) — full-solution `dotnet build --no-incremental` stayed clean (0 errors) and working tree confirmed to contain only the two intended file changes before committing.
**2026-07-24 session, continued: M5 Security Hardening — security headers + request size limiting slice complete (branch `worktree-agent-af0f1d80d81bc9034`).** Scope deliberately narrowed per user request: no JWT-claims/authorization wiring, no `.RequireAuthorization()`, `IsAdmin`/`OwnerId` handling untouched, rate limiting left as-is (already DONE before this slice). Delivered two independent pieces, each its own commit: (1) `SecurityHeadersMiddleware` (`02-src/01-Api/.../Middlewares/`) sets `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin` on every response, plus a strict `Content-Security-Policy: default-src 'none'` — scoped to non-Development environments only, since Scalar's API docs UI (`OpenApiExtensions.UseOpenApiDocumentation`) is HTML with inline scripts/styles and is only ever mapped when `IsDevelopment()` is true; reasoning documented as a code comment on the middleware since it's a genuinely non-obvious tradeoff. `app.UseHsts()` wired into `Program.cs` behind the same `!IsDevelopment()` gate already used for `UseHttpsRedirection`. (2) `RequestSizeLimitMiddleware` rejects any request whose `Content-Length` exceeds a configured ceiling with 413, and also sets `IHttpMaxRequestBodySizeFeature.MaxRequestBodySize` per-request so hosts that bypass Kestrel (TestServer, used by integration tests) enforce the same limit; Kestrel's own `KestrelServerOptions.Limits.MaxRequestBodySize` is set from the same `RequestSizeLimit:MaxBytes` config key in `Program.cs`, defaulting to 1 MB (`RequestSizeLimitExtension.DefaultMaxRequestBodyBytes`) since this API only ever receives JSON metadata — file uploads go direct-to-S3 via presigned URLs (ADR-AR-005), this API never receives raw file bytes. New config key `ConfigurationKeys.RequestSizeLimitMaxBytes` = `"RequestSizeLimit:MaxBytes"`, following the file's existing style (unlike `RateLimit:*`, which predates that convention and stays inline in `RateLimitExtension.cs` — not touched). Two new integration test files: `SecurityHeadersTests.cs` (asserts all four headers on a real response) and `RequestSizeLimitTests.cs` (oversized body → 413, in-limit body not rejected by size limiting). Verified: full non-container suite still 164/164 (70 Domain + 48 Validators + 46 Handlers, unchanged), Integration suite now 14/14 (11 pre-existing + 3 new). Pre-commit hook's full `dotnet build --no-incremental` ran unusually slowly in this worktree (~8+ min per commit, vs. ~2 min for a plain `dotnet build` outside the hook) — root cause not fully diagnosed, suspect the recurring `could not lock config file .../.git/config: File exists` warning (worktree sharing the parent repo's `.git`) adding retry overhead per-project; did not block the work, just slower than expected. Not pushed, no PR opened — stopped after STATE.md/ROADMAP.md update per instructions, for review before merge.
**2026-07-24 session, continued: M5 "API Docs" complete (Scalar UI + OpenAPI 3.1, example bodies, XML doc comments), on a fresh worktree branch off `main`.** Fixed `OpenApiExtensions.cs`'s document `Description` (was the literal scaffold string "API generated with Clean Architecture Template", factually wrong per CLAUDE.md's own history) to a real description of the service. Verified the actual `Microsoft.AspNetCore.OpenApi` 10.0.8 / `Microsoft.OpenApi` 2.10.0 API shape by decompiling both DLLs (`ilspycmd`, installed as a dotnet global tool) rather than guessing: the old `.WithOpenApi(Func<OpenApiOperation,OpenApiOperation>)` extension is `[Obsolete("...", DiagnosticId = "ASPDEPR002")]` in this version — using it would fail the build under `TreatWarningsAsErrors=true` — so examples are added via the still-current `AddOpenApiOperationTransformer(Func<OpenApiOperation, OpenApiOperationTransformerContext, CancellationToken, Task>)` per-endpoint, setting `OpenApiMediaType.Example` (a `System.Text.Json.Nodes.JsonNode`, confirmed via decompile — `Microsoft.OpenApi` 2.x moved off the old `IOpenApiAny`/`OpenApiObject` model). Added realistic request-body examples (values consistent with `ValidationConstants.AssetRules`/`SearchRules`/`MediaRules` bounds — an excavator/crane rental scenario) to all 7 endpoints that take a body, and response examples to the 3 GET-only endpoints (`GetAssetById`/`SearchAssets`/`ListCategories`) — the latter three needed an added `.Produces<T>(StatusCodes.Status200OK)` first, since these endpoints return bare `Task<IResult>` and the OpenAPI generator can't infer a response schema/content without an explicit type annotation (verified empirically: a response `Content` dictionary is only populated once `Produces<T>` is present — confirmed by fetching the live `/openapi/v1.json` before and after). Added `///` XML doc comments to all 10 endpoint classes (`MapEndpoint`, `HandleAsync`, and their private nested `Body` records) and to `OpenApiExtensions`'s two public methods. Enabled `<GenerateDocumentationFile>true</GenerateDocumentationFile>` on the Api csproj; **judgment call, documented inline in the csproj**: also added `CS1591` to `<NoWarn>` rather than writing doc comments for every other public type in the Api project (`Messaging/` consumers+DTOs, `Extensions/` static classes, `Middlewares/`) — none of those are part of the public HTTP surface Scalar renders, and documenting all of them was out of scope for this feature; `TreatWarningsAsErrors=true` meant leaving CS1591 on would fail the build over unrelated files. Verified end-to-end: `dotnet build --no-incremental` clean (0 errors) on the full solution; started the API locally (`dotnet run`, real ports from `launchSettings.json` since `ASPNETCORE_URLS` env didn't override the launch profile) and fetched `/openapi/v1.json` live — valid OpenAPI 3.1.1 JSON, correct title/description, all 7 body-bearing endpoints have a request example, all 3 GET-only endpoints have a response example. Full non-container suite unchanged at 164/164 (70 Domain + 48 Validators + 46 Handlers), Integration still 11/11 — this feature touched no handler/domain/test code, only the Api project's docs/annotations layer.

**2026-07-24 session, continued: F-13 Minimal API Endpoints complete on branch `feat/e05-f13-minimal-api-endpoints` — full spec-driven cycle, M5 kicked off.** 10 HTTP endpoints wired onto the existing Application handlers (Assets: Create, GetById, Search, RequestMediaUpload, ConfirmMediaUpload, SubmitForModeration, AdminReview; Categories: Create, Update, List), all `AllowAnonymous` for now — JWT-claims-derived authorization explicitly deferred to a separate "Security Hardening" feature (user decision this session). Two real gaps found during research and fixed as part of this feature, not silently worked around: (1) no `GetAssetByIdHandler` existed at all despite `IAssetRepository.GetByIdAsync` being available — built one from scratch, matching every other Assets handler's shape but deliberately without a validator (a route-bound `Guid` can't be malformed by the time the handler runs); (2) `ErrorOrExtensions.ToProblem` had no `ErrorType.Forbidden` case, silently falling through to 500 — `CreateAssetHandler`'s existing suspended-owner `Forbidden` result was untestable via HTTP until now and would have 500'd in production. Both fixed and covered by new tests. Also built out `03-tests/05-Integration`'s `CustomWebApplicationFactory` for the first time — it was pure scaffold with a literal `// TODO: Replace infrastructure registrations with fakes` comment; added in-memory fakes (`InMemoryAssetRepository`, `InMemoryCategoryRepository`, `FakeOwnerStatusValidator`, `FakeMediaStorageService`) so HTTP-level endpoint tests run without Docker/AWS, and stripped the 3 Kafka hosted services from the test host (irrelevant to HTTP tests, would otherwise spin retry loops against a nonexistent broker). Found and fixed one more real bug along the way: running two test classes' `WebApplicationFactory` instances in parallel intermittently failed host startup ("entry point exited without ever building an IHost") — root cause is `Program.cs`'s static Serilog bootstrap-logger setup racing across concurrently-invoked entry points; fixed via `[assembly: CollectionBehavior(DisableTestParallelization = true)]`, not a flaky-test workaround. User asked mid-session to relax `.editorconfig`'s forced-explicit-type rules (`csharp_style_var_*` were `false:warning`, i.e. build-breaking) to `true:silent`, allowing `var` — applied repo-wide, not just to new code. Final verification: 70 Domain + 48 Validators + 46 Handlers (44 + 2 new `GetAssetByIdHandlerTests`) = 164/164 non-container, plus 11/11 new integration tests, all green.

**2026-07-24 session, continued: G-006 closed — full `04-Repositories` suite now 29/29 green, two distinct bugs found and fixed along the way (neither was the other).** After the `StopAsync` ordering fix below made `OwnerStatusConsumerTests` pass in isolation, running the *whole* `04-Repositories` suite together still failed `OutboxPublisherTests` — but with a different symptom (`result!.Message.Key` mismatch), proving it was a second, independent bug: `OwnerStatusConsumerTests`/`ModerationVerdictConsumerTests` shared `OutboxFixtureGroup`'s `LocalStackFixture` with `OutboxPublisherTests`. `ModerationVerdictConsumerTests` seeds a `PendingModeration` asset via the real `DynamoDbAssetRepository.SaveAsync`, which — as a side effect — writes a pending Outbox entry (`AssetCreated`) into that *shared* DynamoDB table. When `OutboxPublisherTests` later ran its own `OutboxPublisher` (batch size 25, polls the whole Pending GSI1 partition), it published that leftover entry too, and the test's fresh Kafka consumer (Earliest offset) read the wrong message first. Fixed by giving the two new test classes their own collection (`CrossServiceFixtureGroup`, `03-tests/04-Repositories/.../Fixtures/KafkaFixture.cs`) — separate LocalStack/Kafka containers, no more shared table state with `OutboxPublisherTests`. After that fix, one more (unrelated, pre-existing) flake surfaced: `OutboxPublisherTests`' own hardcoded 15s `Consume` timeout was too tight once the *total* Docker container count for the whole suite doubled (two fixture groups now, each with its own Kafka+LocalStack) — bumped to 30s. Re-ran the full suite: **29/29 passed.** Combined with the already-green 162/162 non-container suite, F-12 is now genuinely fully verified, not just code-complete.

**2026-07-24 session, continued: root cause found and fixed — `OwnerStatusConsumer`/`ModerationVerdictConsumer`'s `StopAsync` called `consumer.Close()` before letting the `Consume()` loop exit.** `Close()` attempts a clean Kafka consumer-group leave, but the poll-loop thread was still blocked inside `Consume(stoppingToken)` when `Close()` was called directly (before `base.StopAsync`), which can hang indefinitely — matches exactly what PR #12's CI run did. Fixed by reordering both classes' `StopAsync`: `await base.StopAsync(cancellationToken)` first (signals `stoppingToken`, which `Consume()` honors and throws `OperationCanceledException`, letting `ExecuteAsync`'s loop exit and the task complete) — only then call `consumer.Close()`. Verified locally with Docker (now available): `OwnerStatusConsumerTests`' 3 tests, previously never completing, now pass in ~13 seconds total. Full `04-Repositories` suite run to confirm no other regressions (see below once it finishes). Comment cleanup also done across F-12's new files per user request — trimmed XML-doc summaries that restated what code does or referenced task/spec paths (CLAUDE.md's own comment policy: only WHY that isn't obvious from the code, and code comments shouldn't reference the task/fix that added them), kept the ones documenting genuinely non-obvious constraints (the `StopAsync` ordering itself, the `OwnerStatusPrefix`/`OwnerPrefix` collision-avoidance note, the real `UserAccountDeleted` vs `UserDeleted` correction).

**2026-07-24 session, continued: PR #12's `build-and-test` CI run (`30101617174`) confirmed genuinely hung, not just slow — manually cancelled.** Re-checked ~30+ minutes after the merge: `Test` step was still `in_progress`, no change from the first check. This rules out "just slow container pulls" and confirms a real bug in this session's untested code — most likely `OwnerStatusConsumerTests`/`ModerationVerdictConsumerTests`' blocking `consumer.Consume(stoppingToken)` loop not honoring cancellation the way the test's `CancellationTokenSource(TimeSpan...)` + `StopAsync` expected. Cancelled via `gh run cancel 30101617174` (user's explicit call) rather than left to run indefinitely — GitHub Actions' default job timeout is 6 hours, so it would not have self-terminated soon. **Next session's first action: reproduce locally with Docker available, fix the cancellation-handling bug, re-run before trusting these tests again** — do not just re-push and hope; the hang is real, not flaky CI.

**2026-07-24 session: F-12 Cross-Service Integration built end-to-end on branch `feat/e04-f12-cross-service-integration`, closing M4/E-04 — merged via PR #12 (https://github.com/eugeniobandeira/rentifyx-asset-registry-api/pull/12).** Full spec-driven cycle (spec.md → design.md → tasks.md → 17 atomic commits). Two Kafka `BackgroundService`s: `OwnerStatusConsumer` (identity-api's `user-lifecycle-events` topic, envelope-wrapped `UserSuspended`/`UserAccountDeleted` — real event name confirmed as `UserAccountDeleted`, not the `UserDeleted` this repo's own docs had assumed) upserts a new DynamoDB-backed owner-status cache; `ModerationVerdictConsumer` (ai-services' `asset-media-moderated` topic, real `SchemaVersion=2` with previously-unknown `Bucket`/`Key` fields — not the `SchemaVersion=1` a prior session's summary and ai-services' own stale design doc both claimed) calls the existing F-09 `ApplyModerationVerdictHandler` per message. Both event contracts were verified against the real sibling-repo source (`rentifyx-identity-api`, `rentifyx-ai-services`, both present locally under `C:\Users\Eugenio\Projects\study\`) rather than assumed — see spec.md's Request/Input section for the corrections this surfaced. New `DynamoDbOwnerStatusValidator` implements both the pre-existing `IOwnerStatusValidator` (Domain-facing read, `CreateAssetHandler` already depended on it since F-05 but it was never registered in DI until now) and a new `IOwnerStatusCacheWriter` (Infrastructure write path) — fail-closed confirmed explicitly by the user this session (missing cache entry = not active), now ADR-AR-011. Keyed DI (`AddKeyedSingleton`/`[FromKeyedServices]`) is net-new to this repo, used to give each consumer its own `IConsumer<string,string>`/`ConsumerConfig.GroupId`. **Verification caveat, stated plainly rather than glossed over:** Docker was unavailable in this session's environment, so the new `03-tests/04-Repositories` integration tests (`OwnerStatusConsumerTests`, `ModerationVerdictConsumerTests`, `DynamoDbOwnerStatusValidatorTests` — all Testcontainers-based, reusing the existing `KafkaFixture`/`LocalStackFixture`) compiled but could **not** be executed locally this session. What *was* verified locally: full solution `dotnet build` clean after every commit (pre-commit hook enforces this), and the non-container suites green with zero regressions — 70 Domain + 48 Validators + 44 Handlers = 162/162. The 04-Repositories suite (previously 20/20 pre-F-12) needs a real run — either locally with Docker available, or via CI on the PR — before this is called fully done. Also flagged and not yet resolved: F-12's cold-start rejection risk (empty owner-status cache on first deploy rejects every `CreateAsset` call) has no backfill plan — see ADR-AR-011's Consequences and spec.md's Open Questions.

**Resume checklist next session:** (1) **first**, check the outcome of Actions run `30101617174` (`gh run view 30101617174`) — did it finish, time out, or get auto-cancelled after the PR merged; (2) if the new Repositories integration tests actually hung in CI, reproduce locally with Docker available and debug — prime suspects are `OwnerStatusConsumer`/`ModerationVerdictConsumer`'s `Consume(stoppingToken)` call not returning promptly on cancellation (Confluent.Kafka's `Consume` can block past a cancelled token in some versions/configurations — worth checking `Confluent.Kafka` 2.15.0's documented cancellation behavior), or the test's own `CancellationTokenSource(TimeSpan...)` timeout being too short for container startup + `StartAsync`/`StopAsync` to complete; (3) once confirmed either fine or fixed, this becomes a normal follow-up commit on `main` (already merged, so no PR ceremony needed for a small fix unless the change is non-trivial); (4) `identity-api`'s `user-lifecycle-events` topic/envelope was flagged in spec.md as young and possibly not fully wired end-to-end on that repo's side — if reproduction shows the consumer genuinely can't connect/consume (not just a cancellation bug), check whether that's a test-setup issue here vs. an actual gap in `identity-api`'s outbox publishing.

**2026-07-23 session, continued: F-10 (PR #9) and F-11 (PR #10) both merged to `master` via admin override — CI's OWASP dependency-check gate could not be made green, see D-003.** After push, both PRs failed the required `build-and-test` check on the OWASP dependency-check step (CVSS≥7 findings). Root cause investigated, not assumed: `Aspire.AppHost.Sdk` (the `<Sdk Name="Aspire.AppHost.Sdk">` MSBuild element in `AppHost.csproj`) was pinned at `9.3.1` while `Directory.Packages.props`' `Aspire.Hosting.AppHost` `PackageVersion` was already `13.4.6` — Sdk elements resolve independently of Central Package Management, so the old Sdk kept pulling its own vulnerable transitive `Google.Protobuf`/`Nerdbank.MessagePack`/`JsonPointer.Net` regardless of CPM pins. This mismatch predated F-10/F-11 (present on `master` before either branch existed — confirmed the last 3 merges to `master` were already CI-red for the same reason). Fixed on both branches: bumped the Sdk to `13.4.6`, enabled `CentralPackageTransitivePinningEnabled` + explicit pins for the worst transitives, bumped the pre-existing `OpenTelemetry` 1.15.x line to 1.17.0 (CVE-2026-39883) and `Microsoft.Extensions.Configuration`/`Options` to 10.0.8 (a floor violation transitive pinning surfaced). This closed most findings but not all — see D-003 for what's left and why admin-merge was the user's explicit call rather than something assumed. F-11's branch also needed a real merge-conflict resolution against `master` after F-10 landed first (`Directory.Packages.props`, `InfrastructureDependencyInjection.cs`, `IoC.csproj` — both branches touched the same files independently; resolved as a union, no logic lost), done in a scratch worktree (`.claude/worktrees/f11-merge`, removed after push). Final state verified on `master` post-merge: `dotnet build` (Release) clean, `dotnet test` 182/182 green (70 Domain + 48 Validators + 44 Handlers + 20 Repositories).

**2026-07-23 session: F-10 (DynamoDB Repository & Outbox) and F-11 (S3 Media Storage) both completed, resuming from the 2026-07-22 mid-session pause.** Task 1 (package refs) verified clean via `dotnet restore`, committed. F-11 was delegated to a background agent working directly in its pre-existing worktree (`.claude/worktrees/agent-a1dde817990a469f5`, branch `feat/e04-f11-s3-media-storage`) since it had no dependency on F-10's work — 5 commits, `S3MediaStorageService` (AWSSDK.S3 4.0.101.4's real API is `GetPreSignedURLAsync`/`GetObjectMetadataAsync`, not `HeadObjectAsync`), `MediaStorageOptions` via `IOptions<T>`, LocalStack-backed integration test. F-10 tasks 2-14 were delegated to a second background agent working in the main checkout (F-10's tasks are explicitly sequential per its own design.md, no parallelism possible within the feature) — 11 commits: DynamoDB options/keys/POCOs/mappers, `PageTokenCodec`, `DynamoDbAssetRepository`/`DynamoDbCategoryRepository`, `KafkaTopics`/`OutboxPublisher`, DI wiring (including `SearchAssetsHandler` catching the new `InvalidPageTokenException` at the Application boundary), AppHost Kafka resource, LocalStack+Kafka integration tests. Both agents' work independently verified afterward: full-solution `dotnet build` (Release) clean, full `dotnet test` 181/181 green (70 Domain + 48 Validators + 44 Handlers + 19 Repositories). ADR-AR-009 (DynamoDB single-table design) and ADR-AR-010 (Outbox poll-loop, correcting the plan doc's wrong "DynamoDB Streams... consistent with identity-api's DEF-005" claim) written. F-12 (Cross-Service Integration) still not started — the only remaining E-04 work. **One process note:** the first attempt to delegate F-11 used the Agent tool's `isolation: "worktree"` option, which creates a brand-new worktree/branch rather than reusing an existing one — this silently put the agent on the wrong branch (missing F-11's spec.md and package-ref changes). Caught before any real work was lost, agent stopped and relaunched without isolation, told to `cd` into the actual pre-existing worktree path directly. Worth remembering: `isolation: "worktree"` is for agents that don't already have a worktree; an agent resuming a specific existing worktree/branch needs a plain (non-isolated) call with the exact path in the prompt.

**2026-07-22 session, part 11 (superseded by the above): E-04 kicked off, 3 features in flight, all uncommitted.** Researched `rentifyx-identity-api` and `rentifyx-communications-api` before starting (not guessed): identity-api's ADR-005 is single-table DynamoDB via `IDynamoDBContext` + custom POCO mappers + GSI-only queries; identity-api's DEF-005 **rejected DynamoDB Streams** for Outbox delivery (no Lambda/compute available) in favor of a `PeriodicTimer` poll-loop `IHostedService` (`OutboxPublisher`, lives in `Api/Messaging/` not Infrastructure, no separate Outbox table) — this **contradicts the plan's own ADR-AR-010 text** ("Streams... consistent with identity-api's DEF-005"), which was wrong and is now corrected. communications-api diverges (low-level `IAmazonDynamoDB`, no Outbox precedent — it's a consumer not producer) but the plan explicitly says "mirrors identity-api ADR-005" so identity-api's pattern is authoritative. User decisions locked in: Outbox = poll-loop (not Streams); F-12's owner-status cache = DynamoDB-backed, not in-memory (multi-pod EKS consistency); F-12's stale-cache fallback = fail-closed (ADR-AR-011, plan's own recommendation, treated as confirmed).

Split into 3 parallel agents (worktrees) per user request, then **user said stop for today mid-flight — all three left with uncommitted, unbuilt/untested work, nothing pushed**:
- **F-10 DynamoDB Repository & Outbox** — branch `feat/e04-f10-dynamodb-repository`, in the **main worktree** (this directory), spec.md committed (`.specs/features/e04-f10-dynamodb-repository/spec.md`, 18 requirements DYN-01..18). design.md + tasks.md written but uncommitted. Started touching `RentifyxAssetRegistry.Api.csproj`, `ApplicationDependencyInjection.cs`, `Directory.Packages.props` — **not built, not tested, do not trust this compiles**. Next: review design.md/tasks.md, verify/redo the package additions (agent's own last words before being stopped: "The Confluent.Kafka reference didn't get added. Let me redo it." — so package wiring was mid-fix), then resume Execute.
- **F-11 S3 Media Storage** — branch `feat/e04-f11-s3-media-storage`, worktree at `.claude/worktrees/agent-a1dde817990a469f5`. spec.md written (`.specs/features/e04-f11-s3-media-storage/spec.md`) but **uncommitted** (untracked). `AWSSDK.S3` package reference added to Infrastructure.csproj + Tests.Repositories.csproj + Directory.Packages.props, uncommitted. Stopped right before writing `MediaStorageOptions`/`S3MediaStorageService` implementation — **no service code written yet**.
- **F-12 Cross-Service Integration** — branch `feat/e04-f12-cross-service-integration` was planned but the agent never got far enough to create its worktree or make any changes — **nothing to resume here, starts from scratch**.

**Resume checklist next session:** (1) `cd` into main worktree, review F-10's design.md/tasks.md and uncommitted diff before continuing — don't assume it's correct, it was written by an agent that got cut off; (2) same for F-11's worktree; (3) verify no duplicate `PackageVersion` entries in `Directory.Packages.props` once both land (both agents were told to add `Confluent.Kafka`/AWS SDK packages independently — check for conflicts before merging either); (4) F-12 needs a fresh start.

**2026-07-22 session, part 10: post-merge cleanup + moderation/ai-services alignment refinement (branch `chore/remove-example-and-ef-scaffold`).** Two independent asks handled in sequence: (1) removed the `dotnet new clean-arch` Example* scaffold and its EF Core/Npgsql Infrastructure layer entirely (D-001 resolved by deletion, not the originally-planned swap) — 58 files, ~1120 lines, `IAddRepository<T>`/`IUpdateRepository<T>`/`IDeleteRepository<T>`/`IUnitOfWork` removed from Domain since nothing but Example used them, `IGetByIdRepository<T>` kept (composed by real `IAssetRepository`/`ICategoryRepository`); EF/Npgsql/Aspire.Hosting.PostgreSQL package refs, the AppHost Postgres resource, and connection-string config all removed too. (2) Reviewed ADR-AR-008 (which the user had written directly on `master` while F-09 work was in flight, citing `rentifyx-ai-services`' real `AssetKeyConventionFilter.cs`/ADR-AI-004) and found a real mismatch: `rentifyx-ai-services`' ADR-AI-004 treats `Approved`/`Rejected` as immediate automated decisions needing no human review — only `PendingReview` goes to a queue — but F-09's `ApplyModerationVerdictHandler` had treated `Rejected` and `PendingReview` identically (both no-op, stuck in `PendingModeration`). User decided: `Rejected` now archives the asset (`AssetEntity.Archive()`, terminal, matches ai-services' intent), same as `AdminReviewAsset`'s manual reject path (previously also a no-op) — this incidentally resolves the "asset stuck in PendingModeration forever" gap without needing a new `AssetStatus` or a revert-to-Draft transition. ADR-AR-008 and F-09's spec.md updated to match; 4 handler tests updated (2 in each of `ApplyModerationVerdictHandlerTests`/`AdminReviewAssetHandlerTests`). Also tightened exception usage during this session (separate from the cleanup): `CategoryEntity.ReParent`/`CreateChild`'s business-rule throws (self-parent, max-depth) were being caught via try/catch in `CreateCategoryHandler`/`UpdateCategoryHandler` — replaced with a pre-check (`CategoryEntity.CanAcceptChild`) so the throw is now an unreachable last-resort guard, not business control flow; extracted `AssetMapper.ToNewAsset(request)` to replace `CreateAssetHandler`'s inline VO+entity construction. Full suite green throughout (70 Domain + 48 Validators + 44 Handlers, zero skips now that Example* is gone).

**2026-07-22 session, part 8: E-03/F-09 Moderation Workflow (US-015) complete (branch `feat/e03-f09-moderation`), M3 fully done.** Verified the real event contract against the `rentifyx-ai-services` sibling repo instead of guessing: it publishes `AssetMediaModerated(AssetId, Verdict, Labels, TopConfidence, Timestamp, SchemaVersion)` where `Verdict` ∈ `{Approved, PendingReview, Rejected}` (ADR-AI-003 thresholds: ≥90% auto-reject, 60–90% manual review, <60% auto-approve). Added a local `ModerationVerdict` enum mirroring those three values — no package dependency on `rentifyx-ai-services`, per ADR-AR-008's event-only boundary. Confirmed via the plan doc: reject/manual-review verdicts leave the asset "held in PendingModeration, admin notified" — no new `AssetStatus` value, no revert-to-Draft path (documented as a known gap, not solved this pass). Delivered: `SubmitForModerationHandler` (owner-only, Draft→PendingModeration, guards status in-handler via `Error.Validation` rather than letting `AssetEntity`'s `InvalidOperationException` propagate), `ApplyModerationVerdictHandler` (the Application-layer contract a future Kafka `IHostedService` consumer will call per event — E-04, not built yet; Approved→Active+`AssetPublished` via existing `AssetEntity.Publish()`, Rejected/PendingReview is a logged no-op; idempotent — any asset not currently `PendingModeration` is treated as an already-processed replay, not an error), `AdminReviewAssetHandler` (manual override, same caller-supplied `IsAdmin` temporary-gap pattern as Categories/F-07). Shared `AssetModerationResponse` across all three. No endpoint added, same E-05 deferral as F-05..F-08. New `AssetErrorCodes.InvalidStatus`/`NotAdmin`. 7 validator tests + 15 handler tests, all green; full suite green.

**2026-07-22 session, part 7: E-03/F-08 Search & Discovery (US-014) complete (branch `feat/e03-f08-search`).** Domain contract correction found while speccing: `AssetSearchFilter`/`ISearchRepository<T,TFilter>` (from E-02) modeled offset pagination (`Page`/`PageSize` + `PagedResult<T>.Total`), which doesn't map to DynamoDB — no native "skip N", and a total match count would require a full table scan. Reworked before building the handler: `AssetSearchFilter` now takes `PageSize`/`NextPageToken` (opaque cursor) + required `Status`, and `ISearchRepository` returns a new `CursorPagedResult<T>(Items, NextPageToken)` instead of `PagedResult<T>` (which stays unchanged for the EF-shaped `IGetAllRepository<T,TFilter>`, unrelated). Delivered: `SearchAssetsHandler` (always hardcodes `AssetStatus.Active` — not caller-controlled — so no non-public asset can leak through search regardless of caller; blank `Keyword`/`NextPageToken` treated as absent), `SearchAssetsValidator` (`pageSize` capped at 30 via new `ValidationConstants.SearchRules`, rejects rather than clamps; `minPrice`/`maxPrice` non-negative and `min<=max`), `SearchAssetsResponse`/`AssetSummaryResponse`. No endpoint added — API layer for all features (Assets/Categories) stays deferred to E-05 per ROADMAP, matching F-05/F-06/F-07 precedent (no `Endpoints/Assets` or `Endpoints/Categories` files exist yet). 12 validator tests + 5 handler tests (Moq), all green; full suite green (Example* skips unaffected).

**2026-07-22 session, part 6: E-03/F-07 Categorization (US-013) complete (branch `feat/e03-f07-categorization`).** Domain gap found while speccing: `CategoryEntity` (E-02) only had `CreateRoot`/`CreateChild` — no rename or re-parent. Discussed scope with user: full re-parent with subtree-depth cascade was ruled out for now — re-parenting is restricted to leaf categories (no children), keeping cycle prevention to a simple self-parent + max-depth check (no ancestor-chain walk needed). Added `CategoryEntity.Rename`/`ReParent`. Delivered: `CreateCategoryHandler`/`UpdateCategoryHandler` (admin-only via caller-supplied `IsAdmin`, same temporary-gap pattern as `OwnerId` in F-05/F-06 pending E-05's real JWT-claims wiring), `ListCategoriesHandler` (public, no gate — caching deferred, no cache infra exists yet). ADR-AR-006 written. 7 validator tests + 11 handler tests (Moq) + 8 new Domain tests for Rename/ReParent, all green.

**2026-07-22 session, part 5: E-03/F-06 Media Upload Pipeline (US-012) complete (branch `feat/e03-f06-media-upload`).** Another domain-contract gap found while speccing: `IMediaStorageService.GeneratePresignedUploadUrlAsync` (E-02) took only `mimeType`/`sizeBytes` — couldn't scope the S3 key to owner+asset as the plan requires. Extended it to take `ownerId`/`assetId` and return a new `PresignedUploadUrl(Url, S3Key)` record. Delivered: `RequestMediaUploadHandler` (validates MIME/size before ever calling `IMediaStorageService`, checks asset existence/ownership), `ConfirmMediaUploadHandler` (re-validates client-supplied MIME/size defensively, attaches `Media` via `AssetEntity.AttachMedia` which raises `AssetMediaUploaded`, saves). New `AssetErrorCodes.NotFound`/`NotOwner`, validation message resource keys. ADR-AR-005 written. 10 validator tests + 9 handler tests (Moq), all green.

**2026-07-22 session, part 4: E-03/F-05 Asset Creation & Idempotency (US-010) complete (branch `feat/e03-f05-create-asset`).** First feature slice of E-03, split out per-feature rather than specifying the whole 5-feature epic at once (E-03 depends on Domain contracts only — E-04/Infrastructure doesn't exist yet, so each feature is its own spec-driven cycle). Two prerequisite fixes made before the feature itself: (1) renamed `IHandler<TRequest,TResponse>.Handle` to `HandleAsync` across the whole codebase (Example* handlers + endpoints) — CLAUDE.md's Async-suffix rule wasn't applied to this shared interface yet; (2) added `AssetEntity.IdempotencyKey` + `IAssetRepository.GetByIdempotencyKeyAsync` (E-02 didn't anticipate this field). Delivered: `CreateAssetHandler` (idempotent replay short-circuits before owner-status check; `Error.Forbidden` for suspended/deleted owners; never publishes to Kafka directly, relies on DynamoDB Streams outbox per ADR-AR-010), `CreateAssetValidator` (mirrors Domain VO bounds), `CreateAssetResponse`/`AssetMapper`, new `AssetErrorCodes`/validation message resource keys (EN + pt-BR). 10 validator tests + 4 handler tests (Moq), all green. US-011 (Draft TTL auto-expiry) deferred — DynamoDB TTL is an E-04 concern.

**2026-07-22 session, part 3: E-01/US-004 Secrets & Cross-Service Auth complete (branch `feat/e01-secrets-jwt`), M1 fully done.** Research into the actual `rentifyx-identity-api` repo found the plan's/CLAUDE.md's original HS256 assumption was wrong — `identity-api` signs with RS256 (its own ADR-006), and no real `ISecretsProvider` interface exists anywhere (only a `SecretsManagerConfigurationProvider`/`AddSecretsManager()` config-builder pattern). Corrected CLAUDE.md and copied the working pattern: secrets load from AWS Secrets Manager via a config provider (skips the AWS call in `Testing` environment, tolerates not-yet-seeded secrets), JWT bearer auth validates RS256 tokens against `identity-api`'s public key. ADR-AR-001 written (superseding the plan's HS256 assumption). `UseAuthentication`/`UseAuthorization` wired in `Program.cs`. Role-based authorization for admin endpoints is explicitly out of scope here (E-05). M1 (E-01) is now fully complete: F-01 scaffold, F-02 CI/CD gates, US-004 secrets/JWT all done; only the EF Core→DynamoDB swap (D-001) remains open, deferred to E-04 by design.

**2026-07-22 session, part 2: E-01/F-02 CI/CD Pipeline & DevSecOps Baseline complete (branch `feat/e01-cicd-devsecops`).** Added OWASP dependency-check (fails on CVSS ≥ 7) and Trivy container scan (fails on CRITICAL/HIGH) to `ci.yml`. Configured `master` branch protection via `gh api`: required status check `build-and-test` + 1 approving review, force-push/delete blocked. Coverage gate (plan's T-016) intentionally NOT added — see D-002. Secrets/JWT slice (US-004, T-020–T-023) deliberately deferred, not part of this pass. This is the first epic run under the new per-epic branch+PR workflow (see feedback memory `feedback_epic-workflow.md`).

**2026-07-22 session, part 1: E-02 Domain Model & Core Asset Logic complete.** Full spec-driven cycle (spec.md/design.md/tasks.md in `.specs/features/e02-domain-model/`), 22 atomic tasks executed via parallel sub-agents where independent, 24 commits. Delivered: `AggregateRoot`/`IDomainEvent`, `AssetStatus`/`MediaUploadStatus` enums, `AssetTitle`/`AssetDescription`/`Money`/`Media` value objects, `AssetCreated`/`AssetMediaUploaded`/`AssetPublished`/`AssetSuspended` domain events, `AssetEntity`/`CategoryEntity` aggregates with full status-lifecycle/depth-cap guards, `IAssetRepository`/`ICategoryRepository`/`IMediaStorageService`/`IOwnerStatusValidator` contracts, ADR-AR-002/003, new `03-tests/00-Domain` test project (60 tests, all green). Zero framework/AWS deps in Domain layer confirmed. Mid-flight architecture refinement: repository interfaces compose new generic `Domain/Interfaces/Common/{ISaveRepository,ISearchRepository,ISoftDeleteRepository,IGetAllRepository<T>}` building blocks rather than the `Example*` scaffold's EF-Core-shaped `IAddRepository`/`IUpdateRepository`/`IDeleteRepository` — documented in CLAUDE.md's "Adding a new feature" section. Next: E-03 (Application layer / use cases).

**2026-07-21 session: project initialized from `RentifyX_AssetRegistryAPI_Plan.md`.** Created `.specs/project/` (PROJECT.md, ROADMAP.md, STATE.md) and root `CLAUDE.md`, following the conventions established in `rentifyx-identity-api` and `rentifyx-communications-api`. No feature work started yet — the repo currently holds only the raw `dotnet new clean-arch` template output (Example* scaffold, EF Core/Npgsql Infrastructure layer, generic `IAddRepository<T>`/`IUnitOfWork`/`AppDbContext`) plus the plan document itself. Turned out the repo already had a local `.git` with 2 prior commits and a GitHub remote (`origin`) — no `git init` was actually needed; docs committed and pushed to `master`.

**2026-07-21 session, part 2: solution/project/namespace renamed from `rentifyx-asset-registry-api`/`rentifyx_asset_registry_api` to `RentifyxAssetRegistry`.** The `dotnet new clean-arch` generator had used the repo's literal folder name for the solution file, all 12 `.csproj` files/directories, and the C# namespace (auto-converted to `rentifyx_asset_registry_api` since C# identifiers can't contain hyphens) — inconsistent with `RentifyxIdentity.*`/`RentifyxCommunications.*` PascalCase convention used in sibling repos. Renamed while the repo still only holds template scaffold (low cost): all `.csproj`/directory names, `.slnx`, `Dockerfile`, `k8s/base/deployment.yaml` (OTEL_SERVICE_NAME), `.github/workflows/ci.yml`, `.hooks/pre-commit`, `README.md`, `CLAUDE.md`. Root repo folder name (`rentifyx-asset-registry-api`, lowercase-hyphen) and the plan doc's `**Repo:**` field were deliberately left untouched — those identify the GitHub repo, not the .NET solution, matching how `identity-api`/`communications-api` keep the two naming schemes separate (lowercase-hyphen for repo/k8s/infra-facing names, PascalCase for .NET solution/namespace). Verified via `dotnet build`/`dotnet test` (Release) after the rename — 12/12 projects build clean, all Example* tests still skip as before (template default), zero failures. **Real pre-existing bug found and fixed along the way, unrelated to the rename itself:** `.hooks/pre-commit` pointed at `$(git rev-parse --show-toplevel)/templates/clean-arch/<solution>.slnx` — a path that has never existed in this repo (leftover from the template generator's own source tree) — meaning the hook would have failed every local commit once `git config core.hooksPath` was ever pointed at `.hooks/`. Fixed to reference the repo-root `.slnx` directly.

## Decisions

| ID | Decision | Rationale | Date |
|---|---|---|---|
| D-003 | F-10/F-11 (PR #9/#10) merged to `master` via `gh pr merge --admin`, bypassing the required `build-and-test` check and the 1-review requirement. | After root-causing and fixing what was fixable (see Current Work above), residual OWASP dependency-check findings had no available fix: `Google.Protobuf`/`Nerdbank.MessagePack`/`OpenTelemetry` have no patched *stable* release yet for the flagged CVEs (only `Nerdbank.MessagePack 1.3.29-beta` addresses its CVEs, not adopted — prerelease); `JsonPointer.Net`'s CVE-2022-4742 is a CPE name-collision false positive (matches an unrelated `json-pointer_project`, not the .NET `gregsdennis/JsonPointer.Net` library); `Microsoft.NET.StringTools.dll`'s CVE-2025-26646 ships with the .NET SDK itself, not a NuGet reference this repo controls. Explicitly confirmed with the user before bypassing (not assumed) via a 3-option prompt (investigate/fix, admin-merge, or suppress via OWASP suppression file) — user chose admin-merge. **Follow-up owed:** an OWASP dependency-check suppression file (`dependency-check-suppressions.xml`) documenting each remaining finding with its specific justification would make the gate honest again instead of permanently red — not yet built, tracked as a Known Gap (G-004). | 2026-07-23 |
| D-002 | CI does NOT enforce an 80% coverage gate (plan's T-016 dropped). CI keeps build+test-pass verification only. | Explicit user decision, stated twice (2026-07-21 and 2026-07-22) — coverage gate adds noise without current value at this project stage | 2026-07-22 |
| D-001 | **RESOLVED 2026-07-22 by removal, not swap.** Template's default EF Core + Npgsql Infrastructure layer (`AppDbContext`, migrations, `IAddRepository<T>`/`IUpdateRepository<T>`/`IDeleteRepository<T>`, `IUnitOfWork`) and the `Example*` scaffold built against it were deleted outright rather than migrated to DynamoDB — neither was ever load-bearing for a real feature. `IAssetRepository`/`ICategoryRepository` still await their DynamoDB implementation in E-04, starting from zero rather than swapping an existing EF implementation. | `dotnet new clean-arch` template is a generic starting point (its own README targets EF Core); this project's plan explicitly calls for DynamoDB single-table design (ADR-AR-009); user decision: no value in keeping dead scaffold around until E-04 | 2026-07-21, resolved 2026-07-22 |

## Blockers

_None active._

## Known Gaps

| ID | Gap | Detail | Since | Resolved |
|---|---|---|---|---|
| G-001 | S3 key convention unconfirmed cross-repo | Both this repo and `rentifyx-ai-services` independently assume `assets/{ownerId}/{assetId}/{filename}` (this repo's `RequestMediaUploadHandlerTests` mock; `rentifyx-ai-services`'s E-02 `AssetKeyConventionFilter`). Resolved by F-11: `S3MediaStorageService` implements exactly this convention (filename is server-generated — Guid + extension from the MIME allow-list, since `IMediaStorageService` doesn't take a caller-supplied filename — flagged as a SPEC_DEVIATION in F-11's spec.md, not a guess). | 2026-07-22 | Yes (2026-07-23) |
| G-002 | ADR-AR-008 was referenced by name (spec.md, ROADMAP.md, this file) three sessions before it was actually written | F-09 shipped citing "per ADR-AR-008" without the file existing in `docs/decisions/`. Written retroactively as `008-moderation-event-only-boundary.md`, matching what F-09 actually implemented (local `ModerationVerdict` enum, `AssetMediaModerated`-only consumption, standalone `AdminReviewAsset`). Worth a process note: write the ADR file in the same commit/PR that first cites it, not after. | 2026-07-22 | Yes (2026-07-22) |
| G-003 | F-09's `Rejected` verdict handling didn't match `rentifyx-ai-services`' own design intent | `rentifyx-ai-services`' ADR-AI-004 treats `Approved`/`Rejected` as immediate automated decisions (no human loop) and only `PendingReview` as needing manual review, but F-09 originally treated `Rejected` identically to `PendingReview` (both no-op, stuck in `PendingModeration`). Fixed: `Rejected` (and `AdminReviewAsset`'s manual reject) now transitions the asset to `Archived` (terminal), matching ai-services' intent and incidentally resolving DEF-003's "stuck forever" framing. | 2026-07-22 | Yes (2026-07-22) |
| G-004 | `master`'s CI is currently red on the OWASP dependency-check step, permanently, until addressed | **RESOLVED.** `dependency-check-suppressions.xml` authored at repo root and wired into `ci.yml`'s `--suppression` arg. Findings verified against the actual `dependency-check-report.html` artifact from CI run `30110991990` (2026-07-24), not guessed: 6 vulnerable dependencies flagged, all CPE false positives or non-fixable — none suppressed on assumption. Suppressed: Google.Protobuf.dll CVE-2026-0994 (matches Python protobuf, not the .NET package; 3.35.1 already latest stable); JsonPointer.Net.dll CVE-2022-4742 (matches unrelated npm `json-pointer`, not gregsdennis/JsonPointer.Net; 7.0.1 already latest); Microsoft.NET.StringTools.dll CVE-2025-26646 (real Microsoft advisory but ships bundled in the .NET SDK toolchain, not a NuGet reference this repo controls/pins); Nerdbank.MessagePack.dll — 13 CVEs (CVE-2026-48109/48502/48506/48509..48517, CVE-2022-41719), all match the separate MessagePack-CSharp project or a Go msgpack library, not Andrew Arnott's Nerdbank.MessagePack; 1.2.36 already latest stable (1.3.29-beta is prerelease only). OpenTelemetry.dll / OpenTelemetry.Instrumentation.AspNetCore.dll CVE-2026-39883 (matches OpenTelemetry-Go, not the .NET SDK; 1.17.0 already latest stable). No package versions were bumped — every flagged package was already at the latest available stable NuGet version, confirmed via the NuGet flat-container API before suppressing rather than assuming no fix existed. Nothing suppressed on guesswork; each `<suppress>` block's `<notes>` cites the specific mismatch found in the report's own CVE description text. Not yet verified against a real CI run from this worktree (feature-branch push wasn't done, per task instructions) — XML validated well-formed only. | 2026-07-23 | Yes (2026-07-24) |
| G-005 | F-12's owner-status cache has no backfill mechanism | `OwnerStatusConsumer` only reacts to live `user-lifecycle-events` messages going forward. On first deploy the cache is empty, and fail-closed (ADR-AR-011) means every `CreateAsset` call is rejected until an owner's cache entry organically exists. No decision made yet on whether `identity-api` should republish a full snapshot, whether this repo should seed `IsActive=true` for all pre-existing owners as a one-time migration, or whether the cold-start rejection window is accepted as-is. | 2026-07-24 | No |
| G-006 | F-12's `04-Repositories` integration tests were written but never verified — and PR #12's CI run appeared stuck on them at merge time | **RESOLVED.** Root cause was a real bug: `OwnerStatusConsumer`/`ModerationVerdictConsumer.StopAsync` closed the Kafka consumer before cancelling the blocking `Consume()` loop, which could hang indefinitely (fixed: cancel via `base.StopAsync` first, `Close()` after). A second, independent bug was also found and fixed: `OwnerStatusConsumerTests`/`ModerationVerdictConsumerTests` shared `OutboxFixtureGroup`'s LocalStack table with `OutboxPublisherTests`, leaking a stray Outbox entry that corrupted that test's Kafka assertion (fixed: dedicated `CrossServiceFixtureGroup`). A minor pre-existing flake (too-tight 15s consume timeout under higher container load) was also bumped to 30s. Full `04-Repositories` suite now 29/29 green. | 2026-07-24 | Yes (2026-07-24) |

## Deferred Ideas

| ID | Idea | Deferred until |
|---|---|---|
| DEF-001 | OpenSearch/ElasticSearch migration for search | Past ~10k assets (ADR-AR-007 watch item) |
| DEF-002 | Booking/availability calendar | Owned by `leasing-api`, explicitly out of scope here |
| DEF-003 | Owner-facing "revert Archived asset back to Draft after rejection" path (resubmission) | Not in plan scope for F-09; rejected assets are now archived (terminal, 2026-07-22 revision — see ADR-AR-008), not stuck — this is about giving owners an edit-and-resubmit path, not about being stuck |

## Open Questions

- **Fail-open vs. fail-closed** when the local owner-status cache (synced from `identity-api` via Kafka) is stale or the event hasn't arrived yet (US-019/T-098). Plan recommends fail-closed (reject creation) given LGPD/marketplace trust considerations — needs explicit confirmation before implementing, not to be assumed.

## Lessons Learned

| ID | Lesson | Context |
|---|---|---|

## Preferences

- All output (code, docs, specs, comments) must be in English (mirrors `identity-api`/`communications-api` convention — carried forward, not yet explicitly confirmed by user for this repo)

## Feature Completion Log

| Feature | Tasks | Tests | Completed |
|---|---|---|---|
| E-02 Domain Model & Core Asset Logic | 22/22 | 60/60 (Tests.Domain) | 2026-07-22 |
| E-01/F-02 CI/CD Pipeline & DevSecOps Baseline | 3/3 (CICD-01/02/03) | n/a (CI config) | 2026-07-22 |
| E-01/US-004 Secrets & Cross-Service Auth | 3/3 (SEC-01/02/03) | n/a (build+config, verified via full test suite) | 2026-07-22 |
| E-03/F-05 Asset Creation & Idempotency (US-010) | CA-01..04 | 14/14 (10 validator + 4 handler) | 2026-07-22 |
| E-03/F-06 Media Upload Pipeline (US-012) | MU-01..04 | 19/19 (10 validator + 9 handler) | 2026-07-22 |
| E-03/F-07 Categorization (US-013) | CAT-01..05 | 26/26 (7 validator + 11 handler + 8 domain) | 2026-07-22 |
| E-03/F-08 Search & Discovery (US-014) | SRCH-01..14 | 17/17 (12 validator + 5 handler) | 2026-07-22 |
| E-03/F-09 Moderation Workflow (US-015) | MOD-01..13 | 22/22 (7 validator + 15 handler) | 2026-07-22 |
| E-04/F-10 DynamoDB Repository & Outbox | 16/16 | 19/19 (Tests.Repositories) + full suite 181/181 pre-merge | 2026-07-23, merged to `master` via PR #9 (admin-merge, see D-003) |
| E-04/F-11 S3 Media Storage | spec-driven, no formal task breakdown | 1/1 (Tests.Repositories) pre-merge | 2026-07-23, merged to `master` via PR #10 (admin-merge, see D-003) |
| E-04/F-12 Cross-Service Integration | 17/17 | 162/162 non-container + 29/29 Repositories (full suite) — all green | 2026-07-24, merged via PR #12, verified same day |
| E-05/F-13 Minimal API Endpoints | 18/18 | 164/164 non-container (70+48+46) + 11/11 new Integration — all green | 2026-07-24 |
| E-05 Security Hardening — security headers + request size limiting slice | 2/2 (headers, size limit; authZ/rate-limiting out of scope) | 164/164 non-container (unchanged) + 14/14 Integration (11 pre-existing + 3 new) — all green | 2026-07-24 |
| E-05/M5 API Docs (Scalar UI + OpenAPI 3.1, examples, XML docs) | n/a (docs/annotations only) | 164/164 non-container + 11/11 Integration — unchanged, all green | 2026-07-24 |
