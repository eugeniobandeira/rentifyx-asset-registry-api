# Project State

## Last Updated

2026-07-23

## Current Work

**2026-07-23 session: F-10 (DynamoDB Repository & Outbox) and F-11 (S3 Media Storage) both completed, resuming from the 2026-07-22 mid-session pause.** Task 1 (package refs) verified clean via `dotnet restore`, committed. F-11 was delegated to a background agent working directly in its pre-existing worktree (`.claude/worktrees/agent-a1dde817990a469f5`, branch `feat/e04-f11-s3-media-storage`) since it had no dependency on F-10's work — 5 commits, `S3MediaStorageService` (AWSSDK.S3 4.0.101.4's real API is `GetPreSignedURLAsync`/`GetObjectMetadataAsync`, not `HeadObjectAsync`), `MediaStorageOptions` via `IOptions<T>`, LocalStack-backed integration test. F-10 tasks 2-14 were delegated to a second background agent working in the main checkout (F-10's tasks are explicitly sequential per its own design.md, no parallelism possible within the feature) — 11 commits: DynamoDB options/keys/POCOs/mappers, `PageTokenCodec`, `DynamoDbAssetRepository`/`DynamoDbCategoryRepository`, `KafkaTopics`/`OutboxPublisher`, DI wiring (including `SearchAssetsHandler` catching the new `InvalidPageTokenException` at the Application boundary), AppHost Kafka resource, LocalStack+Kafka integration tests. Both agents' work independently verified afterward: full-solution `dotnet build` (Release) clean, full `dotnet test` 181/181 green (70 Domain + 48 Validators + 44 Handlers + 19 Repositories). ADR-AR-009 (DynamoDB single-table design) and ADR-AR-010 (Outbox poll-loop, correcting the plan doc's wrong "DynamoDB Streams... consistent with identity-api's DEF-005" claim) written. F-12 (Cross-Service Integration) still not started — the only remaining E-04 work. **One process note:** the first attempt to delegate F-11 used the Agent tool's `isolation: "worktree"` option, which creates a brand-new worktree/branch rather than reusing an existing one — this silently put the agent on the wrong branch (missing F-11's spec.md and package-ref changes). Caught before any real work was lost, agent stopped and relaunched without isolation, told to `cd` into the actual pre-existing worktree path directly. Worth remembering: `isolation: "worktree"` is for agents that don't already have a worktree; an agent resuming a specific existing worktree/branch needs a plain (non-isolated) call with the exact path in the prompt.

**2026-07-22 session, part 11 (superseded by the above): E-04 kicked off, 3 features in flight, all uncommitted.** Researched `rentifyx-identity-api` and `rentifyx-communications-api` before starting (not guessed): identity-api's ADR-005 is single-table DynamoDB via `IDynamoDBContext` + custom POCO mappers + GSI-only queries; identity-api's DEF-005 **rejected DynamoDB Streams** for Outbox delivery (no Lambda/compute available) in favor of a `PeriodicTimer` poll-loop `IHostedService` (`OutboxPublisher`, lives in `Api/Messaging/` not Infrastructure, no separate Outbox table) — this **contradicts the plan's own ADR-AR-010 text** ("Streams... consistent with identity-api's DEF-005"), which was wrong and is now corrected. communications-api diverges (low-level `IAmazonDynamoDB`, no Outbox precedent — it's a consumer not producer) but the plan explicitly says "mirrors identity-api ADR-005" so identity-api's pattern is authoritative. User decisions locked in: Outbox = poll-loop (not Streams); F-12's owner-status cache = DynamoDB-backed, not in-memory (multi-pod EKS consistency); F-12's stale-cache fallback = fail-closed (ADR-AR-011, plan's own recommendation, treated as confirmed).

Split into 3 parallel agents (worktrees) per user request, then **user said stop for today mid-flight — all three left with uncommitted, unbuilt/untested work, nothing pushed**:
- **F-10 DynamoDB Repository & Outbox** — branch `feat/e04-f10-dynamodb-repository`, in the **main worktree** (this directory), spec.md committed (`.specs/features/e04-f10-dynamodb-repository/spec.md`, 18 requirements DYN-01..18). design.md + tasks.md written but uncommitted. Started touching `RentifyxAssetRegistry.Api.csproj`, `ApplicationDependencyInjection.cs`, `Directory.Packages.props` — **not built, not tested, do not trust this compiles**. Next: review design.md/tasks.md, verify/redo the package additions (agent's own last words before being stopped: "The Confluent.Kafka reference didn't get added. Let me redo it." — so package wiring was mid-fix), then resume Execute.
- **F-11 S3 Media Storage** — branch `feat/e04-f11-s3-media-storage`, worktree at `.claude/worktrees/agent-a1dde817990a469f5`. spec.md written (`.specs/features/e04-f11-s3-media-storage/spec.md`) but **uncommitted** (untracked). `AWSSDK.S3` package reference added to Infrastructure.csproj + Tests.Repositories.csproj + Directory.Packages.props, uncommitted. Stopped right before writing `MediaStorageOptions`/`S3MediaStorageService` implementation — **no service code written yet**.
- **F-12 Cross-Service Integration** — branch `feat/e04-f12-cross-service-integration` was planned but the agent never got far enough to create its worktree or make any changes — **nothing to resume here, starts from scratch**.

**Resume checklist next session:** (1) `cd` into main worktree, review F-10's design.md/tasks.md and uncommitted diff before continuing — don't assume it's correct, it was written by an agent that got cut off; (2) same for F-11's worktree; (3) verify no duplicate `PackageVersion` entries in `Directory.Packages.props` once both land (both agents were told to add `Confluent.Kafka`/AWS SDK packages independently — check for conflicts before merging either); (4) F-12 needs a fresh start.

**2026-07-22 session, part 10: post-merge cleanup + moderation/ai-services alignment refinement (branch `chore/remove-example-and-ef-scaffold`).** Two independent asks handled in sequence: (1) removed the `dotnet new clean-arch` Example* scaffold and its EF Core/Npgsql Infrastructure layer entirely (D-001 resolved by deletion, not the originally-planned swap) — 58 files, ~1120 lines, `IAddRepository<T>`/`IUpdateRepository<T>`/`IDeleteRepository<T>`/`IUnitOfWork` removed from Domain since nothing but Example used them, `IGetByIdRepository<T>` kept (composed by real `IAssetRepository`/`ICategoryRepository`); EF/Npgsql/Aspire.Hosting.PostgreSQL package refs, the AppHost Postgres resource, and connection-string config all removed too. (2) Reviewed ADR-AR-008 (which the user had written directly on `master` while F-09 work was in flight, citing `rentifyx-ai-services`' real `AssetKeyConventionFilter.cs`/ADR-AI-004) and found a real mismatch: `rentifyx-ai-services`' ADR-AI-004 treats `Approved`/`Rejected` as immediate automated decisions needing no human review — only `PendingReview` goes to a queue — but F-09's `ApplyModerationVerdictHandler` had treated `Rejected` and `PendingReview` identically (both no-op, stuck in `PendingModeration`). User decided: `Rejected` now archives the asset (`AssetEntity.Archive()`, terminal, matches ai-services' intent), same as `AdminReviewAsset`'s manual reject path (previously also a no-op) — this incidentally resolves the "asset stuck in PendingModeration forever" gap without needing a new `AssetStatus` or a revert-to-Draft transition. ADR-AR-008 and F-09's spec.md updated to match; 4 handler tests updated (2 in each of `ApplyModerationVerdictHandlerTests`/`AdminReviewAssetHandlerTests`). Also tightened exception usage during this session (separate from the cleanup): `CategoryEntity.ReParent`/`CreateChild`'s business-rule throws (self-parent, max-depth) were being caught via try/catch in `CreateCategoryHandler`/`UpdateCategoryHandler` — replaced with a pre-check (`CategoryEntity.CanAcceptChild`) so the throw is now an unreachable last-resort guard, not business control flow; extracted `AssetMapper.ToNewAsset(request)` to replace `CreateAssetHandler`'s inline VO+entity construction. Full suite green throughout (70 Domain + 48 Validators + 44 Handlers, zero skips now that Example* is gone).

**2026-07-22 session, part 8: E-03/F-09 Moderation Workflow (US-015) complete (branch `feat/e03-f09-moderation`), M3 fully done.** Verified the real event contract against the `rentifyx-ai-services` sibling repo instead of guessing: it publishes `AssetMediaModerated(AssetId, Verdict, Labels, TopConfidence, Timestamp, SchemaVersion)` where `Verdict` ∈ `{Approved, PendingReview, Rejected}` (ADR-AI-003 thresholds: ≥90% auto-reject, 60–90% manual review, <60% auto-approve). Added a local `ModerationVerdict` enum mirroring those three values — no package dependency on `rentifyx-ai-services`, per ADR-AR-008's event-only boundary. Confirmed via the plan doc: reject/manual-review verdicts leave the asset "held in PendingModeration, admin notified" — no new `AssetStatus` value, no revert-to-Draft path (documented as a known gap, not solved this pass). Delivered: `SubmitForModerationHandler` (owner-only, Draft→PendingModeration, guards status in-handler via `Error.Validation` rather than letting `AssetEntity`'s `InvalidOperationException` propagate), `ApplyModerationVerdictHandler` (the Application-layer contract a future Kafka `IHostedService` consumer will call per event — E-04, not built yet; Approved→Active+`AssetPublished` via existing `AssetEntity.Publish()`, Rejected/PendingReview is a logged no-op; idempotent — any asset not currently `PendingModeration` is treated as an already-processed replay, not an error), `AdminReviewAssetHandler` (manual override, same caller-supplied `IsAdmin` temporary-gap pattern as Categories/F-07). Shared `AssetModerationResponse` across all three. No endpoint added, same E-05 deferral as F-05..F-08. New `AssetErrorCodes.InvalidStatus`/`NotAdmin`. 7 validator tests + 15 handler tests, all green; full suite green.

**2026-07-22 session, part 7: E-03/F-08 Search & Discovery (US-014) complete (branch `feat/e03-f08-search`).** Domain contract correction found while speccing: `AssetSearchFilter`/`ISearchRepository<T,TFilter>` (from E-02) modeled offset pagination (`Page`/`PageSize` + `PagedResult<T>.Total`), which doesn't map to DynamoDB — no native "skip N", and a total match count would require a full table scan. Reworked before building the handler: `AssetSearchFilter` now takes `PageSize`/`NextPageToken` (opaque cursor) + required `Status`, and `ISearchRepository` returns a new `CursorPagedResult<T>(Items, NextPageToken)` instead of `PagedResult<T>` (which stays unchanged for the EF-shaped `IGetAllRepository<T,TFilter>`, unrelated). Delivered: `SearchAssetsHandler` (always hardcodes `AssetStatus.Active` — not caller-controlled — so no non-public asset can leak through search regardless of caller; blank `Keyword`/`NextPageToken` treated as absent), `SearchAssetsValidator` (`pageSize` capped at 30 via new `ValidationConstants.SearchRules`, rejects rather than clamps; `minPrice`/`maxPrice` non-negative and `min<=max`), `SearchAssetsResponse`/`AssetSummaryResponse`. No endpoint added — API layer for all features (Assets/Categories) stays deferred to E-05 per ROADMAP, matching F-05/F-06/F-07 precedent (no `Endpoints/Assets` or `Endpoints/Categories` files exist yet). 12 validator tests + 5 handler tests (Moq), all green; full suite green (Example* skips unaffected).

**2026-07-22 session, part 6: E-03/F-07 Categorization (US-013) complete (branch `feat/e03-f07-categorization`).** Domain gap found while speccing: `CategoryEntity` (E-02) only had `CreateRoot`/`CreateChild` — no rename or re-parent. Discussed scope with user: full re-parent with subtree-depth cascade was ruled out for now — re-parenting is restricted to leaf categories (no children), keeping cycle prevention to a simple self-parent + max-depth check (no ancestor-chain walk needed). Added `CategoryEntity.Rename`/`ReParent`. Delivered: `CreateCategoryHandler`/`UpdateCategoryHandler` (admin-only via caller-supplied `IsAdmin`, same temporary-gap pattern as `OwnerId` in F-05/F-06 pending E-05's real JWT-claims wiring), `ListCategoriesHandler` (public, no gate — caching deferred, no cache infra exists yet). ADR-AR-006 written. 7 validator tests + 11 handler tests (Moq) + 8 new Domain tests for Rename/ReParent, all green.

**2026-07-22 session, part 5: E-03/F-06 Media Upload Pipeline (US-012) complete (branch `feat/e03-f06-media-upload`).** Another domain-contract gap found while speccing: `IMediaStorageService.GeneratePresignedUploadUrlAsync` (E-02) took only `mimeType`/`sizeBytes` — couldn't scope the S3 key to owner+asset as the plan requires. Extended it to take `ownerId`/`assetId` and return a new `PresignedUploadUrl(Url, S3Key)` record. Delivered: `RequestMediaUploadHandler` (validates MIME/size before ever calling `IMediaStorageService`, checks asset existence/ownership), `ConfirmMediaUploadHandler` (re-validates client-supplied MIME/size defensively, attaches `Media` via `AssetEntity.AttachMedia` which raises `AssetMediaUploaded`, saves). New `AssetErrorCodes.NotFound`/`NotOwner`, validation message resource keys. ADR-AR-005 written. 10 validator tests + 9 handler tests (Moq), all green.

**2026-07-22 session, part 4: E-03/F-05 Asset Creation & Idempotency (US-010) complete (branch `feat/e03-f05-create-asset`).** First feature slice of E-03, split out per-feature rather than specifying the whole 5-feature epic at once (E-03 depends on Domain contracts only — E-04/Infrastructure doesn't exist yet, so each feature is its own spec-driven cycle). Two prerequisite fixes made before the feature itself: (1) renamed `IHandler<TRequest,TResponse>.Handle` to `HandleAsync` across the whole codebase (Example* handlers + endpoints) — CLAUDE.md's Async-suffix rule wasn't applied to this shared interface yet; (2) added `AssetEntity.IdempotencyKey` + `IAssetRepository.GetByIdempotencyKeyAsync` (E-02 didn't anticipate this field). Delivered: `CreateAssetHandler` (idempotent replay short-circuits before owner-status check; `Error.Forbidden` for suspended/deleted owners; never publishes to Kafka directly, relies on DynamoDB Streams outbox per ADR-AR-010), `CreateAssetValidator` (mirrors Domain VO bounds), `CreateAssetResponse`/`AssetMapper`, new `AssetErrorCodes`/validation message resource keys (EN + pt-BR). 10 validator tests + 4 handler tests (Moq), all green. US-011 (Draft TTL auto-expiry) deferred — DynamoDB TTL is an E-04 concern.

**2026-07-22 session, part 3: E-01/US-004 Secrets & Cross-Service Auth complete (branch `feat/e01-secrets-jwt`), M1 fully done.** Research into the actual `rentifyx-identity-api` repo found the plan's/CLAUDE.md's original HS256 assumption was wrong — `identity-api` signs with RS256 (its own ADR-006), and no real `ISecretsProvider` interface exists anywhere (only a `SecretsManagerConfigurationProvider`/`AddSecretsManager()` config-builder pattern). Corrected CLAUDE.md and copied the working pattern: secrets load from AWS Secrets Manager via a config provider (skips the AWS call in `Testing` environment, tolerates not-yet-seeded secrets), JWT bearer auth validates RS256 tokens against `identity-api`'s public key. ADR-AR-001 written (superseding the plan's HS256 assumption). `UseAuthentication`/`UseAuthorization` wired in `Program.cs`. Role-based authorization for admin endpoints is explicitly out of scope here (E-05). M1 (E-01) is now fully complete: F-01 scaffold, F-02 CI/CD gates, US-004 secrets/JWT all done; only the EF Core→DynamoDB swap (D-001) remains open, deferred to E-04 by design.

**2026-07-22 session, part 2: E-01/F-02 CI/CD Pipeline & DevSecOps Baseline complete (branch `feat/e01-cicd-devsecops`).** Added OWASP dependency-check (fails on CVSS ≥ 7) and Trivy container scan (fails on CRITICAL/HIGH) to `ci.yml`. Configured `master` branch protection via `gh api`: required status check `build-and-test` + 1 approving review, force-push/delete blocked. Coverage gate (plan's T-016) intentionally NOT added — see D-002. Secrets/JWT slice (US-004, T-020–T-023) deliberately deferred, not part of this pass. This is the first epic run under the new per-epic branch+PR workflow (see feedback memory `feedback_epic-workflow.md`).

**2026-07-22 session, part 1: E-02 Domain Model & Core Asset Logic complete.** Full spec-driven cycle (spec.md/design.md/tasks.md in `.specs/features/e02-domain-model/`), 22 atomic tasks executed via parallel sub-agents where independent, 24 commits. Delivered: `AggregateRoot`/`IDomainEvent`, `AssetStatus`/`MediaUploadStatus` enums, `AssetTitle`/`AssetDescription`/`Money`/`Media` value objects, `AssetCreated`/`AssetMediaUploaded`/`AssetPublished`/`AssetSuspended` domain events, `AssetEntity`/`CategoryEntity` aggregates with full status-lifecycle/depth-cap guards, `IAssetRepository`/`ICategoryRepository`/`IMediaStorageService`/`IOwnerStatusValidator` contracts, ADR-AR-002/003, new `03-tests/00-Domain` test project (60 tests, all green). Zero framework/AWS deps in Domain layer confirmed. Mid-flight architecture refinement: repository interfaces compose new generic `Domain/Interfaces/Common/{ISaveRepository,ISearchRepository,ISoftDeleteRepository,IGetAllRepository<T>}` building blocks rather than the `Example*` scaffold's EF-Core-shaped `IAddRepository`/`IUpdateRepository`/`IDeleteRepository` — documented in CLAUDE.md's "Adding a new feature" section. Next: E-03 (Application layer / use cases).

**2026-07-21 session: project initialized from `RentifyX_AssetRegistryAPI_Plan.md`.** Created `.specs/project/` (PROJECT.md, ROADMAP.md, STATE.md) and root `CLAUDE.md`, following the conventions established in `rentifyx-identity-api` and `rentifyx-communications-api`. No feature work started yet — the repo currently holds only the raw `dotnet new clean-arch` template output (Example* scaffold, EF Core/Npgsql Infrastructure layer, generic `IAddRepository<T>`/`IUnitOfWork`/`AppDbContext`) plus the plan document itself. Turned out the repo already had a local `.git` with 2 prior commits and a GitHub remote (`origin`) — no `git init` was actually needed; docs committed and pushed to `master`.

**2026-07-21 session, part 2: solution/project/namespace renamed from `rentifyx-asset-registry-api`/`rentifyx_asset_registry_api` to `RentifyxAssetRegistry`.** The `dotnet new clean-arch` generator had used the repo's literal folder name for the solution file, all 12 `.csproj` files/directories, and the C# namespace (auto-converted to `rentifyx_asset_registry_api` since C# identifiers can't contain hyphens) — inconsistent with `RentifyxIdentity.*`/`RentifyxCommunications.*` PascalCase convention used in sibling repos. Renamed while the repo still only holds template scaffold (low cost): all `.csproj`/directory names, `.slnx`, `Dockerfile`, `k8s/base/deployment.yaml` (OTEL_SERVICE_NAME), `.github/workflows/ci.yml`, `.hooks/pre-commit`, `README.md`, `CLAUDE.md`. Root repo folder name (`rentifyx-asset-registry-api`, lowercase-hyphen) and the plan doc's `**Repo:**` field were deliberately left untouched — those identify the GitHub repo, not the .NET solution, matching how `identity-api`/`communications-api` keep the two naming schemes separate (lowercase-hyphen for repo/k8s/infra-facing names, PascalCase for .NET solution/namespace). Verified via `dotnet build`/`dotnet test` (Release) after the rename — 12/12 projects build clean, all Example* tests still skip as before (template default), zero failures. **Real pre-existing bug found and fixed along the way, unrelated to the rename itself:** `.hooks/pre-commit` pointed at `$(git rev-parse --show-toplevel)/templates/clean-arch/<solution>.slnx` — a path that has never existed in this repo (leftover from the template generator's own source tree) — meaning the hook would have failed every local commit once `git config core.hooksPath` was ever pointed at `.hooks/`. Fixed to reference the repo-root `.slnx` directly.

## Decisions

| ID | Decision | Rationale | Date |
|---|---|---|---|
| D-002 | CI does NOT enforce an 80% coverage gate (plan's T-016 dropped). CI keeps build+test-pass verification only. | Explicit user decision, stated twice (2026-07-21 and 2026-07-22) — coverage gate adds noise without current value at this project stage | 2026-07-22 |
| D-001 | **RESOLVED 2026-07-22 by removal, not swap.** Template's default EF Core + Npgsql Infrastructure layer (`AppDbContext`, migrations, `IAddRepository<T>`/`IUpdateRepository<T>`/`IDeleteRepository<T>`, `IUnitOfWork`) and the `Example*` scaffold built against it were deleted outright rather than migrated to DynamoDB — neither was ever load-bearing for a real feature. `IAssetRepository`/`ICategoryRepository` still await their DynamoDB implementation in E-04, starting from zero rather than swapping an existing EF implementation. | `dotnet new clean-arch` template is a generic starting point (its own README targets EF Core); this project's plan explicitly calls for DynamoDB single-table design (ADR-AR-009); user decision: no value in keeping dead scaffold around until E-04 | 2026-07-21, resolved 2026-07-22 |

## Blockers

_None active._

## Known Gaps

| ID | Gap | Detail | Since | Resolved |
|---|---|---|---|---|
| G-001 | S3 key convention unconfirmed cross-repo | Both this repo and `rentifyx-ai-services` independently assume `assets/{ownerId}/{assetId}/{filename}` (this repo's `RequestMediaUploadHandlerTests` mock; `rentifyx-ai-services`'s E-02 `AssetKeyConventionFilter`). Resolved by F-11: `S3MediaStorageService` implements exactly this convention (filename is server-generated — Guid + extension from the MIME allow-list, since `IMediaStorageService` doesn't take a caller-supplied filename — flagged as a SPEC_DEVIATION in F-11's spec.md, not a guess). | 2026-07-22 | Yes (2026-07-23) |
| G-002 | ADR-AR-008 was referenced by name (spec.md, ROADMAP.md, this file) three sessions before it was actually written | F-09 shipped citing "per ADR-AR-008" without the file existing in `docs/decisions/`. Written retroactively as `008-moderation-event-only-boundary.md`, matching what F-09 actually implemented (local `ModerationVerdict` enum, `AssetMediaModerated`-only consumption, standalone `AdminReviewAsset`). Worth a process note: write the ADR file in the same commit/PR that first cites it, not after. | 2026-07-22 | Yes (2026-07-22) |
| G-003 | F-09's `Rejected` verdict handling didn't match `rentifyx-ai-services`' own design intent | `rentifyx-ai-services`' ADR-AI-004 treats `Approved`/`Rejected` as immediate automated decisions (no human loop) and only `PendingReview` as needing manual review, but F-09 originally treated `Rejected` identically to `PendingReview` (both no-op, stuck in `PendingModeration`). Fixed: `Rejected` (and `AdminReviewAsset`'s manual reject) now transitions the asset to `Archived` (terminal), matching ai-services' intent and incidentally resolving DEF-003's "stuck forever" framing. | 2026-07-22 | Yes (2026-07-22) |

## Deferred Ideas

| ID | Idea | Deferred until |
|---|---|---|
| DEF-001 | OpenSearch/ElasticSearch migration for search | Past ~10k assets (ADR-AR-007 watch item) |
| DEF-002 | Booking/availability calendar | Owned by `leasing-api`, explicitly out of scope here |
| DEF-003 | Owner-facing "revert Archived asset back to Draft after rejection" path (resubmission) | Not in plan scope for F-09; rejected assets are now archived (terminal, 2026-07-22 revision — see ADR-AR-008), not stuck — this is about giving owners an edit-and-resubmit path, not about being stuck |

## Open Questions

- **Fail-open vs. fail-closed** when the local owner-status cache (synced from `identity-api` via Kafka) is stale or the event hasn't arrived yet (US-019/T-098). Plan recommends fail-closed (reject creation) given LGPD/marketplace trust considerations — needs explicit confirmation before implementing, not to be assumed.

## Lessons Learned

| ID | Lesson | Context |
|---|---|---|

## Preferences

- All output (code, docs, specs, comments) must be in English (mirrors `identity-api`/`communications-api` convention — carried forward, not yet explicitly confirmed by user for this repo)

## Feature Completion Log

| Feature | Tasks | Tests | Completed |
|---|---|---|---|
| E-02 Domain Model & Core Asset Logic | 22/22 | 60/60 (Tests.Domain) | 2026-07-22 |
| E-01/F-02 CI/CD Pipeline & DevSecOps Baseline | 3/3 (CICD-01/02/03) | n/a (CI config) | 2026-07-22 |
| E-01/US-004 Secrets & Cross-Service Auth | 3/3 (SEC-01/02/03) | n/a (build+config, verified via full test suite) | 2026-07-22 |
| E-03/F-05 Asset Creation & Idempotency (US-010) | CA-01..04 | 14/14 (10 validator + 4 handler) | 2026-07-22 |
| E-03/F-06 Media Upload Pipeline (US-012) | MU-01..04 | 19/19 (10 validator + 9 handler) | 2026-07-22 |
| E-03/F-07 Categorization (US-013) | CAT-01..05 | 26/26 (7 validator + 11 handler + 8 domain) | 2026-07-22 |
| E-03/F-08 Search & Discovery (US-014) | SRCH-01..14 | 17/17 (12 validator + 5 handler) | 2026-07-22 |
| E-03/F-09 Moderation Workflow (US-015) | MOD-01..13 | 22/22 (7 validator + 15 handler) | 2026-07-22 |
| E-04/F-10 DynamoDB Repository & Outbox | 16/16 | 19/19 (Tests.Repositories) + full suite 181/181 | 2026-07-23 |
| E-04/F-11 S3 Media Storage | spec-driven, no formal task breakdown | 1/1 (Tests.Repositories, part of the 181 total) | 2026-07-23 |
